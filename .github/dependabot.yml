version: 2
updates:
  # Keep the GitHub Actions we depend on current (the only dependency surface —
  # the site itself has no package manifests).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
    labels: [dependencies, ci]

# Codex Task Gate

Every Codex task must include an explicit repository PDR path:

```text
PDR_PATH: <exact file path in this repo>
```

If `PDR_PATH` is absent or invalid, stop and request a valid path before any implementation.

Recovery baseline:
`docs/pdr/recovery-pack/HowieZZ_PDR_Recovery_Pack/docs/pdr/PDR-000-recovery-and-source-of-truth.md`

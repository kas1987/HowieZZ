{
  "name": "howiezz-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Test tooling for the HowieZZ / ZELEX Atlas static site.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.9"
  }
}
